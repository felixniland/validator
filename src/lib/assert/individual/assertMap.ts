/**
 * Asserts that a value is a Map.
 * @param v - The value to check
 * @throws Error if the value is not a Map
 */
import { getStdAsserter } from "./_assertUtils.js";
export const assertMap = getStdAsserter("map");
